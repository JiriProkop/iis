{% extends 'base.html.twig' %}

{% block title %}Schedule Activity{% endblock %}

{% block body %}
    {% if error is not null %}
        {{ error }}
    {% endif %}

    {% if activity is not null %}
        <h3>Teacher: {% if activity.teacher is not null %}{{ activity.teacher.login }} <a href="remove_teacher">REMOVE</a>{% endif %}</h3>
        <ul>
            {% for teacher in teachers %}
                <li>{{ teacher.login }} - <a href="set_teacher/{{ teacher.id }}">SET TEACHER</a></li>
            {% endfor %}
        </ul>
    {% endif %}

    {%  if form is not null %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Schedule</button>
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
